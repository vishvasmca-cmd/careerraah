# CareerRaah

A minimal Streamlit MVP to generate personalized career roadmaps.

## Requirements

Install dependencies:

```powershell
pip install -r requirements.txt
```

## Run Locally

```powershell
streamlit run app.py
```

This will open the Streamlit app in your browser at `http://localhost:8501` by default.

